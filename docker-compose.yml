services:
  books:
    build: .
    ports:
      - "8312:8312"
    env_file:
      - .env
    environment:
      - DATABASE_URL=file:/app/data/books.db
    volumes:
      - books-data:/app/data
    restart: unless-stopped

volumes:
  books-data:
